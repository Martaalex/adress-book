const add = document.getElementById('addContact');
const Name = document.getElementById('name');
const phone = document.getElementById('phone');

add.addEventListener("submit", addContact =>{
   
});